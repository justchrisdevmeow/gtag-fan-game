name: Build Godot 4 Quest APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
      
      - name: Install Godot 4.6.2
        run: |
          wget -O godot.zip https://github.com/godotengine/godot/releases/download/4.6.2-stable/Godot_v4.6.2-stable_linux.x86_64.zip
          unzip godot.zip
          chmod +x Godot_v4.6.2-stable_linux.x86_64
      
      - name: Download Android export templates
        run: |
          mkdir -p ~/.local/share/godot/export_templates/4.6.2.stable
          wget -O templates.tpz https://github.com/godotengine/godot/releases/download/4.6.2-stable/Godot_v4.6.2-stable_export_templates.tpz
          cd ~/.local/share/godot/export_templates/4.6.2.stable
          unzip -o $GITHUB_WORKSPACE/templates.tpz
      
      - name: Export APK
        run: |
          mkdir -p build
          ./Godot_v4.6.2-stable_linux.x86_64 --headless --export-release "Android" project.godot
          mv *.apk build/
      
      - uses: actions/upload-artifact@v4
        with:
          name: godot4-apk
          path: build/*.apk
